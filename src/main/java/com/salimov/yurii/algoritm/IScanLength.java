package com.salimov.yurii.algoritm;

/**
 * The interface describes a method for scanning
 * the length of a train.
 *
 * @author Yurii Salimov (yurii.alex.salimov@gmail.com)
 * @version 1.0
 */
public interface IScanLength {

    /**
     * Scans the length of a train.
     *
     * @return The train length (number of wagons).
     */
    int scan();
}
